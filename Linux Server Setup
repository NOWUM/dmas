Requirements:
- Python 3.5 or newer
- Gurobi 8 or newer

Install Gurobi
$ wget http://packages.gurobi.com/VERSION.tar.gz
$ tar xzvf gurobi9.0.1_linux64.tar.gz
$ cd gurobi_VERSION/linux64
$ sudo python3 setup.py install (needs to be done for every user)

Add Gurobi to PATH (execute for every user):
$ export GUROBI_HOME="/home/flo/MAS/gurobi901/linux64"
$ export PATH="${PATH}:${GUROBI_HOME}/bin"
$ export LD_LIBRARY_PATH="${LD_LIBRARY_PATH}:${GUROBI_HOME}/lib"

register gurobi with key
$ grbgetkey KEY


Install python modules (requirements.txt in /home/flo/MAS/)
$ pip3 install -r requirements.txt
includes:
- pandas
- requests
- numba
- tensorflow
- influxDB
- pika
- pymongo
- geohash2
- sklearn
- keras
- pyswarm


*Change syspath for files (Still needed) or make a package from MAS
Actual workaround change .py files:
import sys
sys.path.append('/home/flo/MAS')


In apps/portfolio.py auf Groß und Kleinschreibung achten unter:
# SLP Power & Heat Data
self.G0Ref = self.df = pd.read_pickle(r'./data/profile_**g**0.pkl')
self.RLMRef = self.df = pd.read_pickle(r'./data/profile_**rlm**.pkl')

